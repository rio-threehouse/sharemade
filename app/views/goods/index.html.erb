<div class="row">
  <div class="col-sm-9 col-sm-push-3">
    <div class="home-content good-b-bottom margin-bottom10"><i class="fas fa-star icon good-bg"></i> 注目の作品</div>
      <% if @results == [] %>
        <div class="text-muted eachcontent">
          作品はありません
        </div>
      <% end %>
      <%= render 'results/results', results: @results %>
    </div>

    <%= render 'layouts/home_nav'%>
  </div>
</div>
